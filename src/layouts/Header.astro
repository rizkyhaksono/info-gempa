---
import { title, text } from "../components/primitives"

interface Props {
  titleHeader: string
}

const { pathname } = Astro.url
const { titleHeader } = Astro.props
console.log(pathname)
---

<head>
  <meta charset="UTF-8" />
  <meta name="description" content="Astro description" />
  <meta name="viewport" content="width=device-width" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <meta name="generator" content={Astro.generator} />
  <title>{titleHeader}</title>

  <nav class="bg-white sticky top-0 z-50 bg-opacity-[40%] backdrop-blur-xl backdrop-filter">
    <div class="container flex flex-wrap items-center justify-between mx-auto p-4">
      <a href="/" class="flex items-center space-x-3">
        <img src="https://rizkyhaksono.natee.my.id/favicon.ico" class="h-6" alt="Logo" />
        <span class={title({ color: "foreground", size: "sm" })}>Info Gempa</span>
      </a>
      <button type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-current rounded-lg md:hidden">
        <span class="sr-only">Open main menu</span>
        <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"></path>
        </svg>
      </button>
      <div class="hidden w-full md:block md:w-auto">
        <ul class="font-medium flex flex-col md:p-0 mt-4 border border-gray-100 rounded-lg md:flex-row md:space-x-6 md:mt-0 md:border-0">
          <a href="/" class={text({ size: "sm", class: `${pathname === "/" ? "text-[#18191b] font-medium underline underline-offset-4 hover:underline hover:text-gray-100 duration-300" : "text-gray-500 hover:text-gray-100 duration-300"}` })}
            >Home</a
          >
          <a
            href="/about"
            class={text({ size: "sm", class: `${pathname === "/about" ? "text-[#18191b] font-medium underline underline-offset-4 hover:underline hover:text-gray-100 duration-300" : "text-gray-700 hover:text-[#18191b] duration-300"}` })}
            >About</a
          >
        </ul>
      </div>
    </div>
  </nav>
</head>
